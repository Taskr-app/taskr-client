query GetUserProjects {
  getUserProjects {
    id
    name
    desc
    owner {
      id
      email
      username
      avatar
    }
    members {
      id
      email
      username
      avatar
    }
  }
}
