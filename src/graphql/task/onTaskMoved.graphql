subscription OnTaskMoved($listId: ID!) {
  onTaskMoved(listId: $listId) {
    task {
      id
      pos
      name
      desc
      list {
        id
      }
    }
    moreTasks {
      id
      pos
    }
  }
}
